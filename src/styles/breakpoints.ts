const size = {
  xs: '420px',
  sm: '768px',
  md: '1024px',
  lg: '1280px',
}

export const breakpoints = {
  xsmall: `(max-width: ${size.xs})`,
  small: `(max-width: ${size.sm})`,
  medium: `(max-width: ${size.md})`,
  large: `(max-width: ${size.lg})`,
}
